services:
  db:
    image: postgres:17
    environment:
      POSTGRES_DB: novawing_desk
      POSTGRES_USER: novawing
      POSTGRES_PASSWORD: novawing-dev
    ports:
      - "127.0.0.1:${POSTGRES_PORT:-5432}:5432"
    volumes:
      - desk-db:/var/lib/postgresql/data

volumes:
  desk-db:
